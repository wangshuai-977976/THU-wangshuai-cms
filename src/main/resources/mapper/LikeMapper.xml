<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.wangshuai.dao.LikeMapper">
  	<select id="insertLike">
		insert into cms_like set type_id=#{typeId},
								type=#{type},
								user_id=#{userId},
								status=#{status}  	
  	</select>
  </mapper>